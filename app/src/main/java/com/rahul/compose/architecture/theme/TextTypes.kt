package com.rahul.compose.architecture.theme

import androidx.compose.material3.Typography
import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.text.font.FontWeight

val Typography.bodyExtraSmallBold : TextStyle
    get() = bodyExtraSmall.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Bold
    )

val Typography.bodyExtraSmallMedium: TextStyle
    get() = bodyExtraSmall.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Medium
    )

val Typography.bodyExtraSmallRegular : TextStyle
    get() = bodyExtraSmall.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Normal
    )

val Typography.myBodySmallBold : TextStyle
    get() = myBodySmall.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Bold
    )

val Typography.bodySmallMedium: TextStyle
    get() = myBodySmall.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Medium
    )

val Typography.bodySmallRegular : TextStyle
    get() = myBodySmall.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Normal
    )

val Typography.bodyStandardBold : TextStyle
    get() = bodyStandard.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Bold
    )

val Typography.bodyStandardMedium: TextStyle
    get() = bodyStandard.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Medium
    )

val Typography.bodyStandardRegular : TextStyle
    get() = bodyStandard.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Normal
    )

val Typography.h1Bold : TextStyle
    get() = h1.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Bold
    )

val Typography.h1Medium: TextStyle
    get() = h1.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Medium
    )

val Typography.h1Regular : TextStyle
    get() = h1.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Normal
    )

val Typography.h2Bold : TextStyle
    get() = h2.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Bold
    )

val Typography.h2Medium: TextStyle
    get() = h2.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Medium
    )

val Typography.h2Regular : TextStyle
    get() = h2.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Normal
    )

val Typography.h3Bold : TextStyle
    get() = h3.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Bold
    )

val Typography.h3Medium: TextStyle
    get() = h3.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Medium
    )

val Typography.h3Regular : TextStyle
    get() = h3.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Normal
    )

val Typography.h4Bold : TextStyle
    get() = h4.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Bold
    )

val Typography.h4Medium: TextStyle
    get() = h4.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Medium
    )

val Typography.h4Regular : TextStyle
    get() = h4.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Normal
    )

val Typography.h5Bold : TextStyle
    get() = h5.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Bold
    )

val Typography.h5Medium: TextStyle
    get() = h5.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Medium
    )

val Typography.h5Regular : TextStyle
    get() = h5.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Normal
    )

val Typography.h6Bold : TextStyle
    get() = h6.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Bold
    )

val Typography.h6Medium: TextStyle
    get() = h6.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Medium
    )

val Typography.h6Regular : TextStyle
    get() = h6.copy(
        //fontFamily = FontFamily,
        fontWeight = FontWeight.Normal
    )